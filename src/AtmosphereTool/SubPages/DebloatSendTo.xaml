<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AtmosphereTool.SubPages.DebloatSendTo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AtmosphereTool.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <ScrollView Padding="0,0,10,0" Margin="0,10,0,0">
        <Grid x:Name="ContentArea" Padding="0,0,0,24">
            <StackPanel>
                <Border BorderBrush="DeepSkyBlue" BorderThickness="1" Padding="10,0,10,0" HorizontalAlignment="Left" CornerRadius="2">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE946;" Foreground="DeepSkyBlue"/>
                        <TextBlock Text="You can hide the options by un-checking them." FontSize="18" Margin="10,0,0,0"/>
                    </StackPanel>
                </Border>
                <ItemsControl x:Name="SendToItemsList" ItemsSource="{x:Bind items}" Margin="0,10,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local:SendToItems">
                            <controls:SettingsCard Header="{x:Bind FileName}">
                                <CheckBox IsChecked="{x:Bind NotHidden}" Margin="0,0,-75,0"/>
                            </controls:SettingsCard>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button x:Name="Finish" Content="Apply" Click="SendToItemsApply" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </ScrollView>
</Page>
