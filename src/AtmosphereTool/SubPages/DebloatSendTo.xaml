<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AtmosphereTool.SubPages.DebloatSendTo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AtmosphereTool.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <ScrollView Padding="10,0,10,24">
        <Grid x:Name="ContentArea">
            <StackPanel>
                <Border Background="#222C6f" BorderBrush="#526cfe" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,8,0,8">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE946;" Foreground="#526cfe" FontSize="20"/>
                        <TextBlock x:Name="InfoText" Text="You can disable options by un-checking them." TextWrapping="WrapWholeWords" VerticalAlignment="Center" Foreground="White"/>
                    </StackPanel>
                </Border>
                <ItemsControl x:Name="SendToItemsList" ItemsSource="{x:Bind items}" Margin="0,10,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local:SendToItems">
                            <controls:SettingsCard Header="{x:Bind FileName}">
                                <CheckBox IsChecked="{x:Bind NotHidden}" Margin="0,0,-75,0"/>
                            </controls:SettingsCard>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button x:Name="Finish" Content="Apply" Click="SendToItemsApply" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </ScrollView>
</Page>
